TO BE DONE:
================================================================================
-sredi delete, možda da bude clear ili tako nešto
  - za ceo fullpath, svaku varijantu uradi getValue
  - ako je objekat, vidi koliko ključeva ima
  - ako je array, vidi koliko elemenata ima i da li su svi undefined ili empty
  - sve na desno briši iz errors
-prepend/append revalidira grešku

-keširanje split fullPath-a?
-validacija: kako i kada
-re-validacija: kako i kada
-onBlur eventi

-isDirty: ne radi zbog razlicitih pozicija u array-u console.log(JSON.stringify(newValues) !== defaultValuesJSON);
	const [defaultValuesJSON, setDefaultValuesJSON] = useState('');
	useEffect(() => {
		setDefaultValuesJSON(JSON.stringify(defaultValues));
	}, [defaultValues]);